import { Paper } from "@mutualzz/ui";
import { observer } from "mobx-react";

export const AvatarEditor = observer(() => {
    return (
        <Paper
            elevation={4}
            p="2rem"
            borderRadius={40}
            minWidth={500}
            minHeight={300}
        >
            Coming soon... :3
        </Paper>
    );
});
