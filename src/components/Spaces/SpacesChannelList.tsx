import { Paper } from "@mutualzz/ui-web";
import { observer } from "mobx-react";

export const SpacesChannelList = observer(() => {
    return (
        <Paper elevation={4} p={20} maxWidth="15rem" width="100%">
            test
        </Paper>
    );
});
